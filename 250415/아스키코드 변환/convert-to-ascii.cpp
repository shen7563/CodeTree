#include <iostream>
#include <string>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    char a;
    int b;
    cin >> a >> b;
    cout << (int)a << " " << (char)b;
    return 0;
}